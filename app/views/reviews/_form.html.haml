= simple_form_for(@review) do |f|
  = f.error_notification

  .form-inputs
    = f.input :content, required: true, label: false, placeholder: 'Write a review...', input_html: {class: 'form-control'}
    = f.hidden_field :place_id, required: true, value: @place.id

  .form-actions
    = f.button :submit